# Local serving

- Start the calendar with `serve-calendar.bat` from the project root.
- The script serves the static app on port 8000 and prints the local and shared LAN calendar links.
- Keep its terminal open while serving; closing the terminal stops the server.
- Use the HTTP link (`http://localhost:8000/index.html`), not `file://`, so the calendar data can load.
